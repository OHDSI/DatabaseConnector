#' @param errorReportFile        The file where an error report will be written if an error occurs.
#'                               Defaults to 'errorReportSql.txt' in the directory given by
#'                               `getOption("errorReportFileLocation")`, or the current working
#'                               directory if that option is unset.
